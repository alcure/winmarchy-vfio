#!/bin/bash
# Force-close Looking Glass / virt-viewer even when they hold the keyboard.
set -u
pkill -f 'looking-glass-client' 2>/dev/null || true
pkill -f '/virt-viewer' 2>/dev/null || true
pkill -f '/remote-viewer' 2>/dev/null || true
hyprctl dispatch killactive >/dev/null 2>&1 || true
